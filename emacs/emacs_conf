;max window
(add-to-list 'load-path"~/.emacs.d")
(require 'maxwindow)
;(provide 'maxwindow)

;color theme
;(load-file"~/.emacs.d/olor-theme.el")
;(color-theme-gnome2)

;(add-to-list 'load-path (expand-file-name "~/.emacs.d"))  
;----------------------------------------------------  
;;;;;一般总体设置  
;(require 'general)  
;----------------------------------------------------  
;----------------------------------------------------  
;;;;    c++配置  
;(require 'c++conf)  
;----------------------------------------------------  
;----------------------------------------------------  
;;;;   插件配置  
;(require 'myplugin)  
;----------------------------------------------------  
;----------------------------------------------------  
;;;一些有用的设置  
;(require 'skills)  
;---------------------------------------------------  
